% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_server.R
\name{start_hgnc_mcp_server}
\alias{start_hgnc_mcp_server}
\title{Start HGNC MCP Server}
\usage{
start_hgnc_mcp_server(
  port = 8080,
  host = "0.0.0.0",
  transport = "http",
  swagger = TRUE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{port}{Integer. Port number to run the server on (default: 8080).
Only used when transport is "http".}

\item{host}{Character. Host address to bind to (default: "0.0.0.0").
Only used when transport is "http".}

\item{transport}{Character. Transport mode: "http" for HTTP/SSE transport
or "stdio" for standard input/output (default: "http"). Use "stdio" for
desktop clients like Claude Desktop.}

\item{swagger}{Logical. Whether to enable Swagger UI documentation
(default: TRUE). Only used when transport is "http".}

\item{quiet}{Logical. If TRUE, suppress startup messages (default: FALSE)}

\item{...}{Additional arguments passed to \code{pr$run()} (for HTTP transport)}
}
\value{
The plumber router object (invisibly), for advanced use cases
}
\description{
Initialize and start the HGNC MCP (Model Context Protocol) server using
the plumber2mcp integration. This exposes HGNC nomenclature tools through
the MCP protocol for use by LLM copilots and other MCP clients.
}
\details{
The function performs the following steps:
\enumerate{
\item Loads the Plumber API definition from \code{inst/plumber/hgnc_api.R}
\item Registers MCP prompts (workflow templates) for common HGNC tasks
\item Applies MCP integration via \code{plumber2mcp::pr_mcp()}
\item Starts the server on the specified port
\item Prints connection information
}

The server exposes HGNC functionality through three MCP primitives:
\itemize{
\item \strong{Tools}: API endpoints for actions (search, normalize, validate)
\item \strong{Resources}: Read-only data for context injection (gene cards, metadata)
\item \strong{Prompts}: Workflow templates for multi-step tasks (normalization, compliance)
}

This makes them available to MCP clients like Claude Desktop, VS Code with
MCP extensions, and other compatible applications.
}
\section{Available Tools}{

The MCP server provides the following tools:
\itemize{
\item \code{info}: Get HGNC REST API metadata
\item \code{find}: Search for genes by query
\item \code{fetch}: Fetch gene records by field value
\item \code{resolve_symbol}: Resolve symbols to approved HGNC symbols
\item \code{normalize_list}: Batch normalize gene symbol lists
\item \code{xrefs}: Extract cross-references from gene records
\item \code{group_members}: Get members of a gene group
\item \code{search_groups}: Search for gene groups
\item \code{changes}: Track nomenclature changes since a date
\item \code{validate_panel}: Validate gene panels against HGNC policy
}
}

\section{Available Resources}{

The MCP server provides the following resources for context injection:
\itemize{
\item \code{get_gene_card}: Formatted gene cards (JSON/markdown/text)
\item \code{get_group_card}: Gene group information with members
\item \code{get_changes_summary}: Nomenclature changes since a date
\item \code{snapshot}: Static resource with dataset metadata
}

Resources provide read-only data that can be injected into LLM context
for enhanced understanding of genes, groups, and nomenclature changes.
}

\section{Available Prompts}{

The MCP server provides the following workflow template prompts:
\itemize{
\item \code{normalize-gene-list}: Guide through normalizing gene symbols to HGNC
\item \code{check-nomenclature-compliance}: Validate gene panels against HGNC policy
\item \code{what-changed-since}: Generate human-readable nomenclature change reports
\item \code{build-gene-set-from-group}: Build gene sets from HGNC gene groups
}

Prompts provide structured guidance for multi-step workflows, helping AI
assistants understand how to use multiple tools together to accomplish
complex nomenclature tasks.
}

\section{MCP Client Configuration}{

For HTTP transport, add to your MCP configuration file:

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "mcpServers": \{
    "hgnc": \{
      "url": "http://localhost:8080/mcp"
    \}
  \}
\}
}\if{html}{\out{</div>}}

For stdio transport (recommended for desktop clients), use:

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "mcpServers": \{
    "hgnc": \{
      "command": "Rscript",
      "args": ["-e", "hgnc.mcp::start_hgnc_mcp_server(transport='stdio')"]
    \}
  \}
\}
}\if{html}{\out{</div>}}
}

\examples{
\dontrun{
# Start server with stdio transport (for Claude Desktop)
start_hgnc_mcp_server(transport = "stdio")

# Start HTTP server on default port 8080
start_hgnc_mcp_server()

# Start on custom port
start_hgnc_mcp_server(port = 9090)

# Start without Swagger UI
start_hgnc_mcp_server(swagger = FALSE)

# For programmatic use, capture the plumber object
pr <- start_hgnc_mcp_server(quiet = TRUE)
# ... do something with pr ...
pr$stop()
}
}
