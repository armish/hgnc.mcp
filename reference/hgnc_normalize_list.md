# Build Symbol Index from Cached Data

Build Symbol Index from Cached Data

## Usage

``` r
hgnc_normalize_list(symbols, return_fields = c( "symbol", "name", "hgnc_id", "status", "locus_type", "location", "alias_symbol", "prev_symbol" ), status = "Approved", dedupe = TRUE, index = NULL)
```

## Arguments

- symbols:

  (parameter description)

- return_fields:

  (parameter description)

- status:

  (parameter description)

- dedupe:

  (parameter description)

- index:

  (parameter description)

## Details

This is an automatically generated documentation file. For complete
documentation, run: roxygen2::roxygenise()
